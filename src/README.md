# 👋🏻 Hi!

All the interesting code is inside the `lib` folder. 
The other scripts just use that code for:
- The grid search(es) => grid_search*.py
- The model assessment => model_assessment*.py
- Some analysis on the hyperparameters => show_top_k_cup.py
- The screening phase => screening*.ipynb (these are Jupiter notebooks)

report_monk.py and monitor_gs.py are not relevant, they are just used to, respectively, generate the data for the slides (monk) and monitor the CUP grid search (that took several hours).

At the start of each file, there is a little comment that explains what the script does. Each function is also documented, using the Google format.

*I hope the work will suffice!* 🙂